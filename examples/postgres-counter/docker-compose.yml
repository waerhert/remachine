services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: remachine
      POSTGRES_PASSWORD: remachine
      POSTGRES_DB: remachine
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
